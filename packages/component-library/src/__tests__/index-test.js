import {
  Banner,
  Box,
  Button,
  GlobalStyles,
  H1,
  H2,
  H3,
  H4,
  H5,
  H6,
  HiddenCheckbox,
  ControlledHiddenCheckbox,
  Input,
  ControlledInput,
  Link,
  Tapable,
  Text,
  ThemeProvider,
  BareThemeProvider,
  useTheme,
  useId,
  useMedia,
  useSharedRef,
  VisuallyHidden,
  List,
  ListItem,
  Fieldset,
  ControlledFieldset,
  fieldsetContext,
  RadioButton,
  ControlledRadioButton,
} from '../index.js'

test('Banner is exported', () => {
  expect(Banner).toBeDefined()
})

test('Box is exported', () => {
  expect(Box).toBeDefined()
})

test('Button is exported', () => {
  expect(Button).toBeDefined()
})

test('GlobalStyles is exported', () => {
  expect(GlobalStyles).toBeDefined()
})

test('H1 is exported', () => {
  expect(H1).toBeDefined()
})

test('H2 is exported', () => {
  expect(H2).toBeDefined()
})

test('H3 is exported', () => {
  expect(H3).toBeDefined()
})

test('H4 is exported', () => {
  expect(H4).toBeDefined()
})

test('H5 is exported', () => {
  expect(H5).toBeDefined()
})

test('H6 is exported', () => {
  expect(H6).toBeDefined()
})

test('HiddenCheckbox is exported', () => {
  expect(HiddenCheckbox).toBeDefined()
})

test('ControlledHiddenCheckbox is exported', () => {
  expect(ControlledHiddenCheckbox).toBeDefined()
})

test('Input is exported', () => {
  expect(Input).toBeDefined()
})

test('ControlledInput is exported', () => {
  expect(ControlledInput).toBeDefined()
})

test('Link is exported', () => {
  expect(Link).toBeDefined()
})

test('Tapable is exported', () => {
  expect(Tapable).toBeDefined()
})

test('Text is exported', () => {
  expect(Text).toBeDefined()
})

test('ThemeProvider is exported', () => {
  expect(ThemeProvider).toBeDefined()
})

test('BareThemeProvider is exported', () => {
  expect(BareThemeProvider).toBeDefined()
})

test('useTheme is exported', () => {
  expect(useTheme).toBeDefined()
})

test('useId is exported', () => {
  expect(useId).toBeDefined()
})

test('useMedia is exported', () => {
  expect(useMedia).toBeDefined()
})

test('useSharedRef is exported', () => {
  expect(useSharedRef).toBeDefined()
})

test('VisuallyHidden is exported', () => {
  expect(VisuallyHidden).toBeDefined()
})

test('List is exported', () => {
  expect(List).toBeDefined()
})

test('ListItem is exported', () => {
  expect(ListItem).toBeDefined()
})
test('Fieldset is exported', () => {
  expect(Fieldset).toBeDefined()
})
test('ControlledFieldset is exported', () => {
  expect(ControlledFieldset).toBeDefined()
})
test('fieldsetContext is exported', () => {
  expect(fieldsetContext).toBeDefined()
})

test('RadioButton is exported', () => {
  expect(RadioButton).toBeDefined()
})
test('ControlledRadioButton is exported', () => {
  expect(ControlledRadioButton).toBeDefined()
})
