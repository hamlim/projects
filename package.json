{
  "name": "@matthamlin/projects",
  "private": true,
  "workspaces": [
    "packages/*",
    "sandbox"
  ],
  "scripts": {
    "start": "echo \"no\"",
    "test": "zaps run lerna test",
    "build": "zaps run lerna build",
    "clean-dist": "yarn node scripts/clean-dist.js",
    "create-package": "yarn node scripts/create-package.js",
    "create-site": "yarn node scripts/create-site.js",
    "update-posts": "yarn node scripts/update-posts.js",
    "add-component": "yarn node scripts/add-component.js"
  },
  "devDependencies": {
    "lerna": "^3.4.0",
    "prettier": "1.19.1"
  },
  "resolutions": {
    "react": "experimental",
    "react-dom": "experimental"
  }
}
