{
  "name": "@matthamlin/projects",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "clean-dist": "yarn node scripts/clean-dist.js"
  },
  "devDependencies": {
    "lerna": "^3.4.0",
    "prettier": "^1.19.1"
  }
}
